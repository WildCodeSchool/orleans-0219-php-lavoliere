{% extends 'adminLayout.html.twig' %}

{% block title %}Edition du calendrier{% endblock %}

{% block body %}
    <section>
        <div class="my-5 container">
            <h1 class="admin-h1 my-5 text-center">Edition du calendrier</h1>
            {{ include('calendar/_form.html.twig', {'button_label': 'Modifier'}) }}
            <div class="text-center">
                <a class="btn btn-admin px-4" href="{{ path('calendar_index') }}"
                   role="button">Retour au calendrier</a>
            </div>
            <div class="text-center my-3">
            {{ include('calendar/_delete_form.html.twig') }}
        </div>
        </div>
    </section>
{% endblock %}