{% extends 'visitorLayout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.rawgit.com/openlayers/openlayers.github.io/master/en/v5.3.0/css/ol.css"
          type="text/css">
{% endblock %}

{% block title %}Ferme de la Volière{% endblock %}

{% block body %}

    <header>
        <div>
            <h1 class="text-center title-wrapper">Ferme de la Volière</h1>
            <p class="text-center p-title-wrapper"><a href="#" class="a-header">Vente en ligne</a>
                /
                <a href="#" class="a-header">Cueillette à la ferme</a></p>
        </div>
        <div>
            <img src="{{ asset('build/image_header.jpg') }}" alt="Image d'accueil de la Ferme de la Volière">
        </div>
        <div class="text-center">
            <a class="btn btn-header" href="#"
               role="button">Découvrir notre ferme
            </a>
        </div>
        <div class="header-arrow">
            <a href="#farm-index"></a>
        </div>
    </header>

    <!-- ================= Début Farm Section ================ -->

    <section id="farm-index" class="farm-index">
        <div class="container-fluid">
            <div class="row mx-sm-2 mx-md-5">

                <div class="col-md-12 col-lg-6 my-3 px-md-5">
                    <h2>La volière</h2>
                    <h3 class="h3-farm-index pt-4">Qui sommes-nous&nbsp;?</h3>

                        <div class="pt-4 tabs-custom tabs-horizontal tabs-line" id="tabs-1">
                            <!-- Nav tabs-->
                            <div class="nav-tabs-wrap">
                                <ul class="nav nav-tabs">
                                    <li class="nav-item" role="presentation"><a class="nav-link active" href="#tabs-1-1" data-toggle="tab">Cultures</a></li>
                                    <li class="nav-item" role="presentation"><a class="nav-link" href="#tabs-1-2" data-toggle="tab">Cueillette</a></li>
                                    <li class="nav-item" role="presentation"><a class="nav-link" href="#tabs-1-3" data-toggle="tab">Gîte</a></li>
                                </ul>
                            </div>
                            <!-- Tab panes-->
                            <div class="tab-content">
                                <div class="tab-pane fade show active" id="tabs-1-1">
                                    <p>À la ferme de la Volière, nous respectons les bonnes pratiques agricoles. Ici, pas de label mais du bon sens pour des produits de qualités.</p>
                                    <button class="btn btn-outline">Voir plus...</button>
                                </div>
                                <div class="tab-pane fade" id="tabs-1-2">
                                    <p>Venez retrouver le goût du jardin et de la cueillette à la ferme. Dans un moment calme et au rythme des saisons&nbsp;; en famille ou entre amis.
                                    </p>
                                    <button class="btn btn-outline">Voir plus...</button>
                                </div>
                                <div class="tab-pane fade" id="tabs-1-3">
                                    <p>Vous êtes dans notre région le temps d’un week-end pour un mariage, une fête entre amis ou en semaine pour votre travail&nbsp;? Venez dormir à la ferme&nbsp;!</p>
                                    <button class="btn btn-outline">Voir plus...</button>
                                </div>
                            </div>
                        </div>
                </div>
                <div class="col-md-12 col-lg-6 my-3 px-md-5">
                    <img src="{{ asset('build/image_ferme_1.jpg') }}" class="img-fluid img-big-farm card-img-top"
                         alt="Photo de la Ferme de la Volière">
                </div>
            </div>

            {% if allActualEvents %}
                <div class="discover-events text-center mt-5">
                    <button class="btn btn-outline" data-toggle="collapse" data-target="#farm-events-toggle">
                        Découvrir les actualités
                    </button>
                </div>
            {% endif %}
        </div>

        <div class="card-arrow">
            <div class="icon"></div>
        </div>


    </section>

    <!-- ================= Fin Farm Section ================== -->

    {#    --------------- EVENTS SECTION ---------------#}


    {% if allActualEvents %}

        <section class="farm-events pt-4 pb-2 mb-5">

            <div class="container-fluid collapse" id="farm-events-toggle">
                <h2 class="text-center mt-0 pt-0 pb-2 mb-5">Les Actualités</h2>
                <div class="row justify-content-center">
                    {% for event in allActualEvents %}
                        <div class="single-event card col-lg-5 col-md-12 px-0 justify-content-center w-75 ">
                            <div class="text-center justify-content-center">
                                {% if event.picture %}
                                    <img src="{{ event.picture }}" class="card-img-top pb-2" alt="{{ event.title }}">
                                {% endif %}
                                <div class="card-body mx-auto">
                                    <h3 class="py-2 text-center">{{ event.title }}</h3>
                                    <hr class="text-left col-9 py-2">
                                    <p class="event-date">Du {{ event.startAt|date('d/m/Y') }}
                                        au {{ event.endAt|date('d/m/Y') }}</p>
                                    <div class="text-left">{{ event.description }}</div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>
    {% endif %}

    {#    --------------- WEEK BASKET SECTION ---------------#}

    <section class="week-basket">

        <div class="jumbotron mb-0 d-flex">
            <div class="row justify-content-center mx-auto">
                <h2 class="text-center col-12 mb-5">Le panier de la semaine</h2>
                {% if weekBasket %}
                    <p class="lead col-9">{{ weekBasket.description }}</p>
                    <h3 class="text-center col-12 mb-5">{{ weekBasket.price }} €</h3>
                    <a class="btn btn-over-image" href="#" role="button">Ajouter au panier</a>
                {% else %}
                    <p class="lead text-center col-9">Le panier de la semaine sera disponible dans les prochains
                        jours</p>
                {% endif %}
            </div>
        </div>

    </section>

    {#    --------------- END WEEK BASKET SECTION ---------------#}

    {#    --------------- OUR PRODUCTS SECTION ---------------#}

    <section class="showcased-products">

        <div class="container-fluid">

            <h2 class="text-center mb-5">Nos produits</h2>

            <div class="row justify-content-center">

                <ul class="nav nav-pills mb-4 text-center justify-content-center" id="pills-tab" role="tablist">
                    {% if productsShowcased %}
                        <li class="nav-item m-3 mb-3">
                            <a class="nav-link active" id="pills-home-tab" data-toggle="pill"
                               href="#pills-showcased-products"
                               role="tab"
                               aria-controls="pills-home" aria-selected="true">Tops du moment</a>
                        </li>
                    {% endif %}
                    <li class="nav-item m-3 mb-3">
                        <a class="nav-link" id="pills-profile-tab" data-toggle="pill" href="#pills-harvest" role="tab"
                           aria-controls="pills-profile" aria-selected="false">Calendrier des cueillettes</a>
                    </li>
                </ul>

            </div>

            {% if productsShowcased %}
                <div class="tab-content" id="pills-tabContent">
                    <div class="tab-pane fade show active" id="pills-showcased-products" role="tabpanel"
                         aria-labelledby="pills-showcased-products-tab">
                        <div class="row justify-content-center m-4">

                            {% for product in productsShowcased %}
                                <div class="card text-center mx-4">
                                    <img src="{% if product.picture %}{{ vich_uploader_asset(product, 'pictureFile') }}{% else %}{{ asset('build/image_product_default.jpg') }}{% endif %}"
                                         class="card-img-top" alt="{{ product.name }}">
                                    <div class="card-description mx-auto p-0">
                                        <div class="card-body mx-auto pb-0">
                                            <h5 class="card-title pb-2">{{ product.name }}</h5>
                                            <p class="card-text py-0">
                                                {% if product.description|length > 60 %}{{ product.description [:60] ~ '[...]' }}
                                                {% else %} {{ product.description }}
                                                {% endif %}</p>

                                            <p class="card-text"><span
                                                        class="bold-text">Origine: </span>{{ product.origin }}
                                            </p>
                                        </div>
                                        <div class="card-footer mx-auto py-0">
                                            <p class="card-text price">{{ product.price }}€ <span
                                                        class="card-text-gray">/ {{ product.bundle }}</span></p>
                                        </div>
                                        <div class="card-footer mx-auto">
                                            <a href="#" class="btn btn-outline">Commandez</a>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}

                        </div>

                        <div class="row justify-content-center">
                            <a href="#" class="btn btn-outline btn-width mt-5">Tous nos produits</a>
                        </div>
                    </div>
                </div>
            {% endif %}

            <div class="tab-pane fade" id="pills-harvest" role="tabpanel" aria-labelledby="pills-harvest-tab">
                {#      Calendrier de récoltes      #}
            </div>
        </div>

        </div>
    </section>

    {#    --------------- END OUR PRODUCTS SECTION ---------------#}

    <!-- ================ Début Bannière ======================= -->

    <section class="banner">
        <div class="jumbotron mb-0">
            <div class="container-fluid text-center">
                <img src="{{ asset('build/logo_voliere_complet.png') }}"
                     alt="Logo complet de la ferme de la volière">
                <p class="p-banner">Vente&nbsp;et&nbsp;cueillette&nbsp;à&nbsp;la&nbsp;ferme&nbsp;/ Agriculture&nbsp;raisonnée&nbsp;/
                    Produits&nbsp;régionaux</p>
            </div>
        </div>
    </section>

    <!-- ================ Fin Bannière ========================= -->

    {#    ------------------ MAP/ADDRESS SECTION --------------#}

    <div class="section contact-us">
        <div class="container-fluid">
            <h2 class="text-center pt-3 mb-5">Nous trouver</h2>

            <div class="row address-hours justify-content-center align-items-center">

                <div id="map" class="map col-12 col-lg-3 ml-5 mr-5 mr-lg-0 mb-5">
                    <div id="popup"></div>
                </div>

                <div class="col-12 col-md-4 col-lg-3 mb-2 ml-0 ml-lg-5">
                    <h4 class="mb-1 mt-3">Horaires</h4>

                    <p>8h30-12h30 / 14h-19h (sauf mardi)</p>
                    <p>Fermé les dimanches du 1er novembre au 1er avril</p>
                    <p>Ferme le dimanche 24 décembre à 17h</p>
                    <p>Fermé du 25 décembre au 1er janvier</p>
                </div>

                <address class="col-12 col-md-4 col-lg-3 ml-0 ml-lg-3">
                    <p>
                        <i class="fas fa-map-marker-alt"></i>
                        Ferme de la Volière<br>
                        <span>181 rue de Malvoviers</span><br>
                        <span>45520 Gidy</span>
                    </p>
                    <p><i class="fas fa-phone"></i>
                        Tél. : 06 63 76 75 63
                    </p>
                    <p><i class="fas fa-at"></i>
                        info@lavoliere.info
                    </p>
                </address>

            </div>
            <div class="header-arrow">
                <a href="#"></a>
            </div>
        </div>
    </div>

    {#    ------------------ END MAP/ADDRESS SECTION --------------#}

    {#    --------------- CONTACT FORM SECTION ---------------#}

    <section class="contact" id="contactForm">
        <div class="text-center btn-collapse-contact">
            <button type="button" class="btn btn-outline" data-toggle="collapse" data-target="#collapseContact"
                    aria-expanded="false" aria-controls="collapseContact">Envoyez-nous un message !
            </button>
        </div>
        <div class="container-fluid collapse" id="collapseContact">
            <div class="row justify-content-center">
                <div class="col-md-6 my-5">
                    {{ form_start(form) }}

                    {{ form_label(form.name) }}
                    {{ form_widget(form.name) }}

                    {{ form_label(form.mail) }}
                    {{ form_widget(form.mail) }}

                    {{ form_label(form.phone) }}
                    {{ form_widget(form.phone) }}

                    {{ form_label(form.object) }}
                    {{ form_widget(form.object) }}

                    {{ form_label(form.message) }}
                    {{ form_widget(form.message) }}
                    <div class=" contact-form-validation text-right py-1">
                        <p class="required-field text-left mx-1 px-0 mt-0 mb-1"><span class="required"></span>Champ
                            obligatoire </p>
                        <button type="submit"
                                class="button-form btn btn-dark pr-3 mr-3"
                                id="submit-form-contact">{{ button_label|default('Envoyer') }}</button>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

    </section>

    {#    --------------- END CONTACT FORM SECTION ---------------#}


{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://kit.fontawesome.com/0499915d2e.js"></script>
    <script src="https://cdn.rawgit.com/openlayers/openlayers.github.io/master/en/v5.3.0/build/ol.js"></script>
    {{ encore_entry_script_tags('index') }}
{% endblock %}
