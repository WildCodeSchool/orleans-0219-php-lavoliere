{% extends 'visitorLayout.html.twig' %}

{% block title %}Ferme de la Volière - Votre panier{% endblock %}

{% block body %}
    <section class="shopping-cart">
        <div class="jumbotron">
            <div class="container-fluid">
                <div class="justify-content-center">
                    <h1 class="text-center">Mon panier</h1>
                    {% if user %}
                        <h2 class="text-center">Bonjour {{ user.firstname }} {{ user.lastname }}</h2>
                    {% endif %}
                </div>
            </div>
        </div>
        <div class="container-fluid">
            {% if cart %}
            <div class="table-responsive table-borderless mt-5">
                <table class="table">
                    <thead>
                    <tr class="text-center">
                        <th colspan="3">Description</th>
                        <th scope="col">Prix</th>
                        <th scope="col">Quantité</th>
                        <th scope="col">Total</th>
                        <th scope="col">Supprimer</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for cartProduct in cart %}
                        <tr class="text-center">
                            <td class="img-product my-auto"><img src="{{ cartProduct.product.picture }}" alt="{{ cartProduct.product.name }}"></td>
                            <td class="name-product text-left">{{ cartProduct.product.name }}</td>
                            <td class="description-product text-left my-auto">{{ cartProduct.product.description }}</td>
                            <td class="my-auto">{{ cartProduct.product.price|number_format(2) }} €</td>
                            <td class="my-auto">
                                {% if cartProduct.quantity > 1 %}
                                    <a href="{{ path('cart_decrement', {'id': cartProduct.product.id  }) }}">-</a>
                                {% endif %}
                                {{ cartProduct.quantity }}
                                {% if cartProduct.quantity < 50 %}
                                    <a href="{{ path('cart_increment', {'id': cartProduct.product.id  }) }}">+</a>
                                {% endif %}

                            </td>
                            <td class="my-auto">
                                {{ cartProduct.total }} €
                            </td>
                            <td class="my-auto">
                                {{ include('cart/_delete_form.html.twig') }}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="row total-cart">
                <div class="col-md-4 offset-md-8 text-center col-sm-12">
                <p >Total de votre panier : <span class="total-cart-price">{{ totalCart }} € TTC </span></p>
                </div>
                <div class="col-md-5 offset-md-7 col-sm-12">
                    <a href="{{ path('app_index') }}" class="btn btn-outline-back mt-3 mb-3 col-md-5 mr-1">Continuer les achats</a>
                    <a href="{{ path('delivery') }}" class="btn btn-outline-validate col-md-5 ">Valider la commande</a>
                </div>
            </div>

        </div>
        {% else %}
            <div class="text-center p-5">
                <h3 class="m-5">Votre panier est vide !</h3>
                <button class="btn btn-outline-back m-3">Continuer les achats</button>
            </div>
        {% endif %}
    </section>
{% endblock %}
{% block javascripts %}
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script lang="js">
        function onClickDecrement(event){
            event.preventDefault();
            const
        }
    </script>
    {{ parent() }}
{% endblock %}
